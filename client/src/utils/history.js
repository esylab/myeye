// exports a new instance of the createBrowserHistory function from the history library, which creates a new history object that can be used to manage the browser's history. This object can be imported and used in other components to navigate the app.



import { createBrowserHistory } from 'history';

export default createBrowserHistory();
